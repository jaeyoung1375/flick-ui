# API 계약 (API Contract)

> 사실 정본은 [`flick-ui-api-contract.md`](./flick-ui-api-contract.md)입니다. **단, 엔드포인트의 존재·요청/응답 필드 자체의 정본은 `flick-server` 저장소의 `docs/ai/09_api_contract/flick-server-api-contract.md`입니다.** 이 디렉토리는 flick-ui가 실제로 무엇을 호출하는지(소비자 관점)를 정리하며, 두 문서가 어긋나면(백엔드에 없는 엔드포인트를 프론트가 호출하는 등) `12_known_issues`에 기록합니다.
