# 데이터 모델 (Data Model)

> 사실 정본은 [`flick-ui-data-model.md`](./flick-ui-data-model.md)입니다. 이 저장소에는 DB가 없다 — 여기서 다루는 건 클라이언트 쪽 타입·전역 상태 shape이다. **DB 스키마·테이블 정의의 정본은 `flick-server` 저장소의 `docs/ai/10_data_model/flick-server-data-model.md`**이며, 이 문서에서 추측으로 채우지 않는다.
